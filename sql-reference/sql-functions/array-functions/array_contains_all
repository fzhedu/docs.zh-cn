# array_contains_all

## 功能

检查数组中是否包含另外一个数组的所有元素元素，是的话返回1，否则返回0.

不关心元素的顺序；
null 视为值；

## 语法

```Haskell
array_contains_all(any_array, any_array)
```

## 示例

```plain text
mysql> select array_contains_all([1,3,4],[3,1,4]);
+------------------------------------------+
| array_contains_all([1, 3, 4], [3, 1, 4]) |
+------------------------------------------+
|                                        1 |
+------------------------------------------+

mysql> select array_contains_all([1,3,4],[3,null]);
+------------------------------------------+
| array_contains_all([1, 3, 4], [3, NULL]) |
+------------------------------------------+
|                                        0 |
+------------------------------------------+
1 row in set (0.01 sec)

mysql> select array_contains_all([1,3,4,null],[3,null]);
+------------------------------------------------+
| array_contains_all([1, 3, 4, NULL], [3, NULL]) |
+------------------------------------------------+
|                                              1 |
+------------------------------------------------+
1 row in set (0.01 sec)

mysql> select array_contains_all([1,3,4,null],[]);
+-----------------------------------------+
| array_contains_all([1, 3, 4, NULL], []) |
+-----------------------------------------+
|                                       1 |
+-----------------------------------------+
1 row in set (0.01 sec)

mysql> select array_contains_all([null],[]);
+--------------------------------+
| array_contains_all([NULL], []) |
+--------------------------------+
|                              1 |
+--------------------------------+
1 row in set (0.01 sec)

mysql> select array_contains_all([],[]);
+----------------------------+
| array_contains_all([], []) |
+----------------------------+
|                          1 |
+----------------------------+
1 row in set (0.01 sec)
```
